<%= form_with(model: bingo_game) do |form| %>
  <% if bingo_game.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(bingo_game.errors.count, "error") %> prohibited this bingo_game from being saved:</h2>

      <ul>
        <% bingo_game.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
